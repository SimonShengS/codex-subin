# Publish self-contained Profile Packages

Codex Subin publishes each recommended Profile as a complete seven-agent package instead of a base package plus an override chain. This duplicates unchanged role files, but makes manual installation and agent-assisted application explicit and independent; repository review must therefore preserve identical role semantics across Profiles and limit differences to the declared model and reasoning-effort mappings.
