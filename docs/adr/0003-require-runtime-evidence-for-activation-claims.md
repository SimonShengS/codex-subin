# Require runtime evidence for activation claims

Codex Subin never treats a TOML file, filename, task name, expected mapping, or child self-report as proof that a custom role was activated. Installation and profile changes are accepted through a read-only Runtime Probe that compares the requested role with host-side session metadata, actual model and effort, and injected role instructions; anything the active interface cannot expose remains explicitly unobservable.
